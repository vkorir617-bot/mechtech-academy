<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>MechTech Academy</title>

<style>
:root {
  --primary:#0a4cff;
  --dark:#0f172a;
  --light:#f1f5f9;
  --accent:#22c55e;
}

* {
  box-sizing: border-box;
  font-family: "Segoe UI", sans-serif;
}

body {
  margin: 0;
  background: var(--light);
  color: #111;
}

/* HEADER */
header {
  background: var(--dark);
  color: white;
  padding: 20px;
}

header h1 {
  margin: 0;
}

nav {
  margin-top: 10px;
}

nav a {
  color: #cbd5f5;
  margin-right: 15px;
  text-decoration: none;
}

nav a:hover {
  color: white;
}

/* HERO */
.hero {
  padding: 60px 20px;
  background: linear-gradient(135deg, var(--primary), #1e40af);
  color: white;
  text-align: center;
}

.hero h2 {
  font-size: 2.5em;
}

.hero p {
  max-width: 600px;
  margin: auto;
}

/* SEARCH */
.search-box {
  max-width: 400px;
  margin: 30px auto;
  display: flex;
}

.search-box input {
  flex: 1;
  padding: 10px;
  border: none;
  border-radius: 5px 0 0 5px;
}

.search-box button {
  padding: 10px;
  border: none;
  background: var(--primary);
  color: white;
  border-radius: 0 5px 5px 0;
  cursor: pointer;
}

/* COURSES */
.container {
  padding: 30px;
  max-width: 1200px;
  margin: auto;
}

.grid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(250px,1fr));
  gap: 20px;
}

.card {
  background: white;
  border-radius: 10px;
  padding: 20px;
  box-shadow: 0 4px 15px rgba(0,0,0,0.1);
  transition: transform 0.3s;
}

.card:hover {
  transform: translateY(-5px);
}

.card h3 {
  color: var(--primary);
}

.card button {
  background: var(--accent);
  color: white;
  border: none;
  padding: 8px 12px;
  border-radius: 5px;
  cursor: pointer;
}

/* MODAL */
.modal {
  display: none;
  position: fixed;
  inset: 0;
  background: rgba(0,0,0,0.6);
  justify-content: center;
  align-items: center;
}

.modal-content {
  background: white;
  padding: 30px;
  max-width: 600px;
  width: 90%;
  border-radius: 10px;
}

.close {
  float: right;
  cursor: pointer;
  font-size: 20px;
}

/* FOOTER */
footer {
  background: var(--dark);
  color: white;
  text-align: center;
  padding: 15px;
}
</style>
</head>

<body>

<header>
  <h1>⚙️ MechTech Academy</h1>
  <nav>
    <a href="#">Home</a>
    <a href="#">Courses</a>
    <a href="#">Products</a>
    <a href="#">Contact</a>
  </nav>
</header>

<section class="hero">
  <h2>Learn Mechanical Products & Technology</h2>
  <p>Engines, gears, welding, electronics, automation and real-world mechanical systems.</p>
</section>

<div class="search-box">
  <input type="text" id="search" placeholder="Search mechanical topics...">
  <button onclick="search()">Search</button>
</div>

<section class="container">
  <div class="grid" id="courseGrid">

    <div class="card" data-title="Engine Technology">
      <h3>Engine Technology</h3>
      <p>IC engines, diesel, petrol, maintenance & diagnostics.</p>
      <button onclick="openLesson('Engine Technology','Learn how engines convert fuel into motion, parts of IC engines, lubrication, cooling systems.')">View Lesson</button>
    </div>

    <div class="card" data-title="Gear Systems">
      <h3>Gear Systems</h3>
      <p>Spur, helical, bevel, worm gears and applications.</p>
      <button onclick="openLesson('Gear Systems','Understand torque transmission, gear ratios, efficiency and industrial uses.')">View Lesson</button>
    </div>

    <div class="card" data-title="Welding Technology">
      <h3>Welding Technology</h3>
      <p>MMAW, MIG, TIG, safety & electrode selection.</p>
      <button onclick="openLesson('Welding Technology','Metal joining techniques, welding machines, safety and real projects.')">View Lesson</button>
    </div>

    <div class="card" data-title="Mechanical Electronics">
      <h3>Mechatronics</h3>
      <p>Sensors, motors, automation & robotics.</p>
      <button onclick="openLesson('Mechatronics','Combine mechanics with electronics, PLCs, actuators and control systems.')">View Lesson</button>
    </div>

  </div>
</section>

<!-- MODAL -->
<div class="modal" id="modal">
  <div class="modal-content">
    <span class="close" onclick="closeModal()">✖</span>
    <h2 id="modalTitle"></h2>
    <p id="modalText"></p>
  </div>
</div>

<footer>
  © 2025 MechTech Academy | Learn. Build. Innovate.
</footer>

<script>
function openLesson(title, text) {
  document.getElementById("modalTitle").innerText = title;
  document.getElementById("modalText").innerText = text;
  document.getElementById("modal").style.display = "flex";
}

function closeModal() {
  document.getElementById("modal").style.display = "none";
}

function search() {
  let q = document.getElementById("search").value.toLowerCase();
  document.querySelectorAll(".card").forEach(card => {
    let title = card.dataset.title.toLowerCase();
    card.style.display = title.includes(q) ? "block" : "none";
  });
}
</script>

</body>
</html>
